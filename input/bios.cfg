MEMORY {
    FULL: start = $0000, size = $10000, file = %O, fill = yes;
}

SEGMENTS {
    STARTUP:   load = FULL, type = ro, start = $FF00;
    BIOS:      load = FULL, type = ro, start = $FF90;
    VECTORS:   load = FULL, type = ro, start = $FFFA;
}