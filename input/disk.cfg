MEMORY {
    FULL: start = $0000, size = $80000, file = %O, fill = yes;
}

SEGMENTS {
    BOOT: load = FULL, type = ro, start = $0000;
    ROOT: load = FULL, type = ro, start = $0100;
    USER: load = FULL, type = ro, start = $0300;
}
