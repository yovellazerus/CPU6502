MEMORY {
    FULL: start = $0000, size = $10000, file = %O, fill = yes;
}

SEGMENTS {
    RODATA: load = FULL, type = ro, start = $C000;
    LIB: load = FULL, type = ro, start = $D000;
    MONITOR: load = FULL, type = ro, start = $E000;
    BIOS:   load = FULL, type = ro, start = $F000;
    VECTORS: load = FULL, type = ro, start = $FFFA;
}